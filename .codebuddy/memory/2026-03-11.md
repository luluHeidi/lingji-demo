# 2026-03-11 工作记录

## 权益审查字段完整升级
- 将权益详情从旧的20个字段升级为完整的50+待审查字段
- 字段按逻辑分为8个分组：基本信息、授权期限、转授权信息、授权平台与区域、二创权利、二创创作成果、二创传播限制、审核与判定、分销与特殊说明
- 每组有带图标的分组标题
- 二创创作成果和二创传播限制分组仅在"二创权利-权利有无"为"有"时展示
- 更新了 data.json 中 sampleRightsDetail 的数据结构以匹配新字段

## 权益字段展示优化
- 将所有字段从 input readonly 改为纯文本 span 展示，去掉可修改交互
- 空值统一显示为 '-'，解决字段值看不见的问题
- 新增 .rrd-rf-value 样式：浅灰背景 + 深色文字，对比度清晰
- 部署预览：http://0918687bdc06488ab2c668b732486eb4.ap-singapore.myide.io

## 合同对照工具
- 在权益ID卡片头部的版权类型标签后添加「查看原合同」按钮
- 点击按钮右侧拉出合同对照抽屉面板（580px宽，含遮罩层）
- 为 data.json 中全部6条权益数据（RR-UGC-001 的3条 + RR-UGC-002 的3条）各生成一篇合同原文样例
- 合同原文包含左侧待审字段的全部字段信息，按条款分段展示
- 合同段落样式：标题居中加蓝色底线，条款标题加蓝色竖线标记，签章行灰色
- 抽屉面板结构：元信息区（权益ID/合同号/合同类型）+ 合同原文区
- 部署预览：http://ef28fa1418684e0fad07e1ee3f97d886.ap-singapore.myide.io

## 修复：data.json JSON语法错误导致页面空白
- 合同原文中3处 `简称"本作品"` 的ASCII双引号未转义，导致JSON解析失败
- 整个授权管理模块因数据加载失败而无法渲染（首页项目列表和授权审查全部空白）
- 修复方案：将ASCII双引号替换为中文引号 `简称"本作品"`
- 部署预览：http://8cbd234135094ec9a3b6e5168483f73e.ap-singapore.myide.io

## 合同对照工具：悬浮抽屉改为分屏模式
- 移除旧的悬浮浮层抽屉（fixed定位+遮罩层），改为内嵌分屏布局
- 展开权利卡片后，点击「查看原合同」在右侧50%区域展示合同内容
- 左侧权利字段区域缩小为50%，网格从3列变为2列（字体大小和行间距不变）
- 合同面板高度与左侧权利区域等高，超长合同通过滚动条上下查看
- 「查看原合同」按钮仅在权利展开时（展开区域底部）才出现，收起时不可见
- 使用JS class切换（rrd-split-active）确保跨浏览器兼容
- 响应式：小屏(<1024px)时分屏改为上下堆叠
- 部署预览：http://a9e527bcb9284e7fa3c483199fa91a5d.ap-singapore.myide.io

## 合同对照工具：按钮位置和高度修复
- 按钮位置：从展开区域底部（rrd-split-left-actions）移回header中，紧跟权益ID+版权类型标签后
- 收起时隐藏：通过CSS `.collapsed .rrd-btn-header-contract { display: none }` 实现
- 高度修复：syncSplitHeight 使用双重 requestAnimationFrame 确保布局回流完成后计算
  - 之前问题：单层rAF时布局还没从3列→2列回流完成，高度按全宽计算导致偏矮
  - 修复后：双重rAF等待分屏缩宽布局稳定，scrollHeight 为缩宽2列后的真实高度
- 部署预览：http://da75013e9f9341aaa1f20794588b901f.ap-singapore.myide.io

## 合同对照工具：高度对齐方案优化
- 新方案：左侧字段区域缩宽后**不改变高度**（保持缩宽前全宽3列布局的高度），超出内容通过滚动条查看
- 右侧合同面板高度**对齐左侧缩宽前的高度**，不再按缩宽后增高的高度计算
- 实现逻辑：先在分屏激活前（全宽3列状态）读取 `leftPanel.scrollHeight` 作为 `originalHeight`，然后再激活分屏，最后将左右面板均设为此固定高度
- syncSplitHeight(card, fixedHeight)：fixedHeight>0 时设置固定高度+滚动；fixedHeight=0 时清除限制
- 三个关闭场景（按钮toggle关闭、关闭按钮、收起卡片）均调用 `syncSplitHeight(card, 0)` 清除高度
- 添加左侧面板滚动条美化样式（5px宽、圆角、半透明灰）
- 部署预览：http://1737e71c1f55483782b55a57addabfff.ap-singapore.myide.io

## 合同对照工具：左右面板等高对齐修复
- 问题：右侧合同面板白色区域比左侧字段白色区域矮，没有对齐
- 根因：之前用 `leftPanel.scrollHeight` 获取高度，分别设置左右面板高度，但这个值可能不等于容器的实际渲染高度
- 修复方案：改为在 `rrd-split-layout` 容器级别设固定高度
  - 用 `splitLayout.offsetHeight`（缩宽前的完整容器渲染高度）作为固定高度
  - 在 `splitLayout` 上设置 `height` + `overflow: hidden`
  - 左右面板通过 flex `align-items: stretch` 自动填满容器，实现真正等高
  - 左侧加 `overflow-y: auto` 支持滚动查看超出内容
- 部署预览：http://7e07b624b76f44af8af4b1b1839fd3e7.ap-singapore.myide.io

## 合同对照工具：左右面板等高对齐修复（第二次）
- 问题：上次修复仍未生效，右侧合同白色区域比左侧矮很多
- 根因分析：之前在**分屏激活前**（3列布局）获取 `splitLayout.offsetHeight`，但3列布局时高度比分屏后的2列布局矮得多（内容从3列重排到2列会增高）
- 修复方案：
  - **先激活分屏**（添加 `rrd-split-active` class + 渲染合同面板）
  - 然后用 `requestAnimationFrame` 等待布局重排完成
  - 在 rAF 回调中测量 `leftPanel.scrollHeight`（此时已是2列布局的真实高度）
  - 将此高度设置到 `splitLayout` 容器上
- 同时将右侧面板背景从灰色 `#f8f9fa` 改为白色 `#ffffff`
- `contract-inline-wrapper` 和 `contract-inline-body` 也加了白色背景
- 部署预览：http://c35a66f39a25467f859194ffa743151a.ap-singapore.myide.io

## 合同对照工具：抽拉式Toggle按钮 + 关键词搜索高亮
- **优化1 - 抽拉式按钮**：
  - 从 header 中移除「查看原合同」按钮
  - 在左侧字段区域顶部右侧新增 `.rrd-contract-toggle-btn` 按钮
  - 默认状态：双箭头向左图标 + "合同对照"文字 + 左侧脉冲动画条提示
  - 激活状态：双箭头向右图标 + "收起"文字 + 蓝色实填背景
  - 通过 CSS class `active` 切换图标和文字的显隐
- **优化2 - 关键词搜索**：
  - 在合同面板 header 中增加搜索框（`.contract-search-bar`）
  - placeholder 为"搜索关键词"，带搜索图标 + 匹配计数 + 清除按钮
  - 输入关键词后 200ms 防抖执行搜索，使用 TreeWalker 遍历文本节点
  - 匹配项用黄色高亮 `.contract-search-highlight`
  - 自动滚动到第一个匹配项位置
  - 支持 Escape 快捷键清除搜索
- 部署预览：http://c42bf9946ae74fdaaec7ec6c647f5112.ap-singapore.myide.io

## 合同对照按钮：改为右侧贴边悬浮样式
- 将 toggle 按钮从 `rrd-contract-toggle-bar` div 容器中提出，直接放在 `rrd-split-left` 内部
- 按钮改为 `position: absolute; right: 0; top: 50%; transform: translateY(-50%)` 悬浮定位
- 按钮竖向排列（`flex-direction: column`），文字用 `writing-mode: vertical-rl` 竖排
- 圆角：左侧高圆角 10px，右侧低圆角 2px（贴边），`border-radius: 10px 2px 2px 10px`
- 右边框去掉（`border-right: none`），贴合 split-left 的右边缘
- 脉冲动画条移到按钮右侧（`right: -1px`）
- `rrd-split-left` 添加 `position: relative` 作为定位参考
- 部署预览：http://5027741e2b99474187923b3661381841.ap-singapore.myide.io

## 权益复审节点：增加"授权依据的引入权利"单选题
- 在 node-2（权益复审）面板中，人审结论之前新增"授权依据的引入权利"单选问题
- 选项来自当前 IP 下所有待审权益的 rightsId（通过 `getPendingRightsIds()` 函数从 `sampleRightsDetail[currentReviewItem.id]` 获取）
- 如果只有一个待审权益，默认自动选中该选项
- 提交校验：提交审查结论前必须选择授权依据的引入权利，否则提示"请先选择授权依据的引入权利"
- 已审核面板展示：node-2 已审核时展示所选的授权依据权益ID（mono 字体）
- data.json 中 node-2 增加 `authBasisRightsId` 字段
- 部署预览：http://78512b0cb2c543a2ab088ca068a03aa8.ap-singapore.myide.io

## 待审权利字段分区重组
- 按用户要求重新组织字段分区，从原来9个分区调整为6个分区：
  1. **基本信息**（4字段）：版权权益ID、权益合同号、版权类型、付费类型
  2. **授权相关**（14字段，新分区，图标key）：授权权利、授权权利合同条款、授权性质、转授权许可、转授权限定条件、转授权其他限定条件、转授权起止日期、授权区域、其他授权区域、授权平台特殊说明、授权平台合同条款、剪辑权限定条件、剪辑权其他限定条件、剪辑时长要求
  3. **二创权利**（不变）
  4. **二创创作成果**（不变，条件展示）
  5. **二创传播限制**（不变，条件展示）
  6. **分销相关**（3字段）：分销平台、分销终端、分销平台特殊说明（去掉特殊说明和特殊说明备注）
- 删除了：授权期限分区、转授权信息分区、授权平台与区域分区、审核与判定分区
- 原分散在多个分区的授权相关字段合并到新的"授权相关"分区
- 部署预览：http://c0125e0e549e439ea5fbcf5a448c3469.ap-singapore.myide.io

## 待审权利字段改为枚举值tag样式展示
- 所有字段从纯文本展示改为"枚举值标签"样式：展示所有可能的枚举值作为tag，当前字段值以蓝色选中样式高亮
- 新增 FIELD_ENUMS 枚举映射表，覆盖17个有固定枚举值的字段（copyrightType、payType、authRights、authNature、sublicenseAllowed 等）
- 新增渲染函数：
  - `rfEnum(label, value, fieldKey, fullWidth, isMono)`：有预设枚举值的字段，展示所有枚举tag，当前值高亮选中；值不在枚举中时额外展示虚线边框自定义tag
  - `rfText(label, value, fullWidth, isMono)`：自由文本字段（如合同条款、备注），值以单个选中tag展示
- 枚举tag CSS：未选中灰底灰字，选中蓝底蓝字+蓝边框+粗体；空值虚线边框；紧凑模式适配
- 原 rf/rfMono/rfFull 函数保留但不再用于待审权利区域
- 部署预览：http://86047c1c9cf640acb40bf6591d54e548.ap-singapore.myide.io

## 枚举值选中样式改为圆点选中
- 去掉枚举tag的背景色和边框，改为纯文本 + 圆点指示器样式
- 未选中：灰色小圆圈（6px，空心，灰色边框）+ 灰色文字
- 选中：实心蓝色圆点 + 蓝色文字加粗 + 淡蓝色外发光
- 去掉 padding/border-radius/background，大幅减少每个枚举项的占用空间
- 枚举项间距从 gap: 6px 缩小为 gap: 2px 10px（行间距2px，列间距10px）
- 空值占位隐藏圆点，直接显示 "-"
- 紧凑模式（分屏）间距进一步缩小为 1px 8px
- 部署预览：http://093ce84ab5874826b76166140778056b.ap-singapore.myide.io

## 修复审核节点三"可授权平台"字段行间距不一致
- 问题：审核节点三中，可授权平台字段与其他选项字段（授权判断、不授权原因等）之间的行间距不一致，视觉错乱
- 根因：可授权平台的 `rrd-panel-row` 在 `node3-auth-platform-fields` 容器内是 last-child，被 `rrd-panel-row:last-child { margin-bottom: 0 }` 规则命中导致下方间距为0
- 修复：添加 CSS 规则让 `node3-defect-disposal-fields`、`node3-deny-fields`、`node3-auth-platform-fields` 三个条件容器内的 `rrd-panel-row` 始终保持 `margin-bottom: 14px`，覆盖 last-child 规则
- 部署预览：http://63f018f7dfee413e86fd116f501e1dd8.ap-singapore.myide.io

## 审核节点三：增加授权起止时间必填字段
- 在节点三"可授权平台"下方新增"授权起止时间"日期范围选择，仅在选择"可授权"时显示
- 两个日期 input（type=date），中间用"—"分隔，样式为原生日期选择器
- **默认填充逻辑**：授权开始日期默认 = max(今天, 权利数据中所有authStartDate的最大值)，通常默认为今天
- **日期约束**：开始日期和结束日期的 min 属性设为今天，过去时间置灰不可选；结束日期的 min 还会跟随开始日期变化
- **提交校验**：可授权时两个日期都必填，结束日期必须晚于开始日期
- **数据保存**：保存 authDateStart（YYYY-MM-DD）、authDateEnd、authDateDisplay（xxxx年xx月xx日 - xxxx年xx月xx日 格式）
- **已审核展示**：在只读面板中展示"授权起止时间"行，显示中文格式日期
- data.json node-3 增加 authDateStart、authDateEnd、authDateDisplay 字段
- CSS 新增 .node3-date-range-wrap、.node3-date-input、.node3-date-separator 样式
- 部署预览：http://1a5c1310a76c42bf8e92e74b57116f1b.ap-singapore.myide.io

## 授权起止时间改为按平台分别展示
- **需求变更**：授权起止时间从单一全局字段改为依赖可授权平台勾选结果，每个勾选平台各出现一条独立的授权起止时间必填行
- **交互逻辑**：
  - 未勾选任何平台时，不显示任何日期行
  - 勾选「快手」→ 出现「快手授权起止时间」必填行
  - 勾选「抖音」→ 出现「抖音授权起止时间」必填行
  - 取消勾选时对应日期行消失
- **实现方案**：
  - 删除旧的 `node3-auth-date-fields` 静态容器，改为在 `node3-auth-platform-fields` 内添加 `node3-platform-date-container` 动态容器
  - 新增 `updatePlatformDateFields(container)` 函数：根据已勾选的checkbox动态渲染/销毁对应平台的日期行，保留已填写的值
  - 新增 `initPlatformDateDefaults(dateRow)` 函数：为每个平台日期行初始化默认开始日期和min约束
  - checkbox 添加 change 事件监听，调用 updatePlatformDateFields
- **数据模型**：node.platformDates 改为对象 `{ "快手": { start, end, display }, "抖音": { start, end, display } }`
- **提交校验**：遍历所有 `.node3-platform-date-row`，按平台名称提示缺失
- **已审核展示**：按平台分行展示，每行标签为「{平台名}授权起止时间」
- 部署预览：http://06f5b3975cf245659baae0ea1621c9b6.ap-singapore.myide.io

## 三级审核节点改为左右分栏布局
- **布局变更**：原来的横向节点条+下方面板改为左右分栏布局
  - 左侧 1/3：审核流程时间轴，标题为"审核流程"
  - 右侧 2/3：当前节点审核选项面板，标题为当前节点名称
  - 中间有竖线分隔
- **左侧时间轴样式**：
  - 竖线+圆形节点样式，每个节点显示名称、角色、审核时间
  - 未到达节点 = 灰色圆点（灰色背景+灰色边框）
  - 已结束节点 = 浅蓝色圆点（淡蓝背景+蓝色边框）+ ✓ 标记
  - 进行中节点 = 深蓝色圆点（实填蓝色+外发光）
  - 已审核节点的竖线连接线也为蓝色
  - 审核完成后，结果信息以标签形式展示在时间轴节点下方
- **右侧面板**：
  - 标题动态显示当前查看的节点名
  - 面板内容（审核选项/已审核只读展示）与之前完全一致
- **交互适配**：
  - 左侧时间轴节点可点击切换右侧面板（同旧节点条逻辑）
  - 提交后自动切换到下一节点，左侧时间轴高亮+右侧标题同步更新
  - 修改按钮点击后，左侧时间轴高亮+右侧标题同步更新
- **响应式**：小屏(≤900px)时左右分栏改为上下堆叠
- 部署预览：http://9a7cb92e74eb4da299a5a6ba486346b4.ap-singapore.myide.io

## 修复：审核节点右侧面板被合同对照CSS覆盖
- **问题**：合同对照分屏布局(style.css第5925行)使用了相同的.rrd-split-right选择器且设置display:none，覆盖了审核节点右侧面板
- **修复**：给审核节点分栏样式加上`.rrd-review-nodes`祖先选择器提高优先级，并用`display: flex !important`确保不被覆盖
- 部署预览：http://c2e9685d673e4e57a24115b766cef5e7.ap-singapore.myide.io

## 审核节点分栏布局修复：左侧固定宽度+背景色区隔
- 问题：左侧33%宽度不够导致文字被挤成竖排，布局错乱
- 修复：左侧改为固定240px宽度，去掉竖线div改用右侧border-left区隔，背景色差异（灰/白）区分
- 部署预览：http://ec6cad541f7444d4bc6d385921289449.ap-singapore.myide.io

## 文案和字段优化
- **审核节点一改名**：data.json中node-1的name从"综控初审"改为"权益初审"，同步更新main.js中所有"综控初审待审"为"权益初审待审"（reviewStatus、progressMap、默认返回值）
- **去掉付费类型字段**：基本信息模块中删除 `rfEnum('付费类型', ri.payType, 'payType')` 行
- **授权相关改名**：sectionTitle从"授权相关"改为"授权基础权利"
- **按钮改名**：首页按钮从"添加确权IP"改为"添加审查IP"
- **弹窗字段调整**：弹窗标题"添加确权IP"→"添加审查IP"，"待审查权益ID"→"待审合同号"，placeholder和title中"权益ID"→"合同号"
- **风险提示调整**："人工添加确权IP"→"人工添加审查IP"
- **校验提示调整**：错误提示中"权益ID"→"合同号"
- 部署预览：http://7124a638cf4b4316b71f6debd7f19c08.ap-singapore.myide.io

## 弹窗文案二次修正（之前替换未完全生效）
- index.html弹窗标题：**"添加确权IP"→"添加审查IP"**
- index.html弹窗label：**"待审查权益ID"→"待审查合同号"**
- index.html弹窗placeholder/title：**"多条权益ID请用；分隔"→"多条合同号请用；分隔"**
- main.js校验注释&错误提示：**"权益ID"→"合同号"**
- style.css注释：**"添加确权IP"→"添加审查IP"**，**"待审查权益ID"→"待审查合同号"**
- 部署预览：http://e187cde1cdc248ba91ac7d713751f3f8.ap-singapore.myide.io

## 授权审查列表新增审核任务ID搜索
- `rrFilters` 新增 `taskIdText` 字段
- 筛选栏右侧新增审核任务ID搜索框（等宽字体），位于作品名搜索框左侧
- `filterRRItems` 新增 taskId 模糊匹配逻辑
- 事件绑定：回车键搜索 + 失焦（点击页面空白处）搜索，与作品名搜索框的防抖实时搜索行为区分
- style.css 新增 `.rr-search-taskid` 样式（等宽字体、200px宽度）
- 部署预览：http://7bba15d6dfd54f6099c2cc0ad7b39d3b.ap-singapore.myide.io

## 搜索框改造：撤回独立任务ID搜索框，复用原搜索框
- 撤回之前新增的独立审核任务ID搜索框（HTML模板中未成功添加，JS事件绑定和CSS样式已清理）
- 删除 `bindRRFilterEvents` 中 `taskId` 事件绑定代码
- 删除 `style.css` 中 `.rr-search-taskid` 样式
- 修改原搜索框 placeholder：**"搜索作品名称..."→"搜索作品名称/任务ID..."**
- `filterRRItems` 搜索逻辑增加 `taskId` 字段匹配（与作品名、别名同时模糊搜索）
- 部署预览：http://68eb398bde724b2584c146d51a23cce9.ap-singapore.myide.io

## v0.9.0 版本管理 - 授权管理模块完整版
- 创建版本快照 `versions/v0.9.0/`（23个文件，792KB）
  - auth-management/（5个文件：index.html, style.css, main.js, data.json, config.json）
  - material-query/（10个文件，v0.8.0锁定版）
  - 主框架文件（index.html, css/, js/）
  - MODULE_REGISTRY.json + VERSION_NOTES.md
- 更新 MODULE_REGISTRY.json：version→0.9.0，auth-management 标记 locked:true + versionSnapshot
- 更新 版本记录.md：新增 v0.9.0 完整版本说明
- 更新 VERSION_SUMMARY.md：全面更新为v0.9.0信息，项目完成度 2/20=10%
