# 端口管理快速上手 🚀

> **一页搞定端口管理问题**

---

## 🎯 核心规则（必读）

| 端口 | 用途 | 状态 | 能否使用 |
|------|------|------|---------|
| **3000** | 宠爱应用 | 🔒 保留 | ❌ 绝对不可以 |
| **5173** | 灵机系统主端口 | ✅ 可用 | ✅ 推荐使用 |
| **5174-5176** | 灵机系统备用 | ✅ 可用 | ✅ 备选 |

---

## ⚡ 快速启动（3步）

### 1️⃣ 启动开发服务器

```bash
cd /Users/baoshengongxiaolu/CodeBuddy/IP权益变现应用_20260107
./scripts/start-dev.sh
```

**自动完成**：
- ✅ 检查端口可用性
- ✅ 自动选择可用端口（5173-5180）
- ✅ 启动服务器
- ✅ 显示访问地址

### 2️⃣ 打开浏览器访问

```
http://localhost:5173
```

### 3️⃣ 停止服务器

```
按 Ctrl + C
```

---

## 🔧 常见问题秒解

### ❓ 端口被占用？

```bash
# 检查端口
./scripts/check-port.sh 5173

# 如果被占用，使用备用端口
python3 -m http.server 5174
```

### ❓ 预览不可见？

**检查清单**：
1. ✓ 服务器是否在运行？`lsof -i:5173`
2. ✓ 地址是否正确？`http://localhost:5173`
3. ✓ 防火墙是否阻止？
4. ✓ 浏览器缓存清理？`Cmd + Shift + R`

### ❓ 需要清理旧端口？

```bash
./scripts/clean-ports.sh
```

---

## 📋 手动启动（备选方案）

### Python HTTP Server

```bash
# 启动
python3 -m http.server 5173

# 访问
http://localhost:5173

# 停止
Ctrl + C
```

### npx serve

```bash
# 启动
npx serve -l 5173

# 访问
http://localhost:5173
```

---

## 🎨 端口一览表

```
┌─────────────────────────────────────────┐
│  端口分配图                               │
├─────────────────────────────────────────┤
│  3000      宠爱应用       [不可动]       │
│  ┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈        │
│  5173      灵机系统主端口  [推荐]        │
│  5174      灵机备用1      [可用]        │
│  5175      灵机备用2      [可用]        │
│  5176      灵机预览端口   [可用]        │
│  5177-5180 灵机扩展      [可用]        │
│  ┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈        │
│  8100-8110 测试环境      [临时]        │
│  9000-9010 备用端口      [应急]        │
└─────────────────────────────────────────┘
```

---

## 💡 最佳实践

1. **统一使用脚本启动** - `./scripts/start-dev.sh`
2. **优先使用5173** - 保持端口一致性
3. **及时停止服务** - 开发完成后 Ctrl+C
4. **定期检查端口** - 每周一次
5. **记录端口使用** - 更新文档

---

## 📚 完整文档

- [端口管理方案.md](./端口管理方案.md) - 详细管理方案
- [端口清理报告.md](./端口清理报告.md) - 清理记录
- [scripts/README.md](./scripts/README.md) - 脚本说明

---

## 🆘 紧急情况

### 所有端口都被占用

```bash
# 1. 批量检查
for port in 5173 5174 5175; do
  lsof -i:$port | grep LISTEN
done

# 2. 强制清理（谨慎使用）
./scripts/clean-ports.sh
```

### 找不到占用进程

```bash
# 使用 sudo 强制清理
sudo lsof -ti:5173 | xargs sudo kill -9
```

---

## ✅ 验证清单

在开发前，快速检查：

- [ ] 端口 3000 未被使用（宠爱应用专属）
- [ ] 端口 5173 可用（主开发端口）
- [ ] 脚本有执行权限（`chmod +x scripts/*.sh`）
- [ ] 服务器启动成功
- [ ] 浏览器可访问预览

---

**🎉 现在，端口管理问题已彻底解决！**

*收藏本页，随时查阅 📌*
